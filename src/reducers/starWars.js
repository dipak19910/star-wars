const initialState = {
  data: [],
  filteredData: [],
  searchValue: ''
}
export default function starWars (state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
